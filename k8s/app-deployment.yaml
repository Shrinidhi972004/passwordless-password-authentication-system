apiVersion: apps/v1
kind: Deployment
metadata:
  name: passwordless-app
  namespace: passwordless-auth
spec:
  replicas: 2
  selector:
    matchLabels:
      app: passwordless-app
  template:
    metadata:
      labels:
        app: passwordless-app
    spec:
      containers:
      - name: passwordless-app
        image: shrinidhiupadhyaya/passwordless-password-authentication-system-passwordless-auth:latest
        ports:
        - containerPort: 10000
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        env:
        - name: MONGO_USER
          valueFrom:
            secretKeyRef:
              name: passwordless-secrets
              key: MONGO_USER
        - name: MONGO_PASS
          valueFrom:
            secretKeyRef:
              name: passwordless-secrets
              key: MONGO_PASS
        - name: MONGODB_URI
          value: mongodb://$(MONGO_USER):$(MONGO_PASS)@mongo:27017
        - name: SESSION_SECRET
          valueFrom:
            secretKeyRef:
              name: passwordless-secrets
              key: SESSION_SECRET
        - name: RP_ID
          value: localhost
        - name: ORIGIN
          value: http://localhost:10000
        - name: PORT
          value: "10000"
